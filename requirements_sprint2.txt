# Sprint 2 依赖
# 用于AI功能和异步处理

# HTTP客户端（用于API调用）
httpx==0.25.2

# 数据验证
pydantic==2.5.0

# 异步支持（如果使用asyncio）
# Python 3.7+ 内置

# 可选：用于更好的日志
loguru==0.7.2

# 可选：用于测试
pytest==7.4.3
pytest-asyncio==0.21.1

# 注意：
# - colorama 用于CLI颜色显示（可选）
# - json, datetime, random 等都是Python标准库
# - 如果要使用真实的DeepSeek API，需要配置API密钥
